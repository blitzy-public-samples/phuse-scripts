# PhUSE Scripts - R Environment Configuration
# This file bootstraps the renv package management system.
# On first setup, run: renv::restore()
# This will install all packages pinned in renv.lock.

source("renv/activate.R")
